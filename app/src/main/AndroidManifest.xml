<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.igor.apptcc">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".AppTccAplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".usuario.LoginActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".usuario.NovoLoginActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".listagem.ListagemActivity"
            android:label="@string/title_activity_listagem"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden" />

        <!-- ANTIGO -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme" />

        <service
            android:name=".service.LocationUpdatesService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".CadastroEstabActivity"
            android:label="@string/title_activity_cadastro_estab"
            android:theme="@style/AppTheme" />

        <service
            android:name=".service.FetchAddressIntentService"
            android:exported="false" />

        <activity
            android:name=".CadastroEstabMapaActivity"
            android:label="@string/title_activity_cadastro_estab_mapa"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NewEstabActivity"
            android:label="@string/title_activity_new_estab"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_new_estab"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignupActivity"
            android:label="@string/title_activity_new_estab"
            android:theme="@style/AppTheme" />

        <service
            android:name=".service.FetchLocationIntentService"
            android:exported="false" />

        <receiver
            android:name=".broadcastReceiver.TokenBroadcastReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".EstabActivity"
            android:label="@string/title_activity_estab"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".utils.NewDrink"
            android:label="@string/title_activity_new_drink"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NewAvaliableActivity"
            android:label="@string/title_activity_new_avaliable"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SearchDrinkActivity"
            android:label="@string/title_activity_search_drink"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".estabelecimento.NomeEstabelecimentoActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".estabelecimento.EnderecoEstabelecimentoActivity"
            android:label="@string/title_activity_endereco_estabelecimento"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".estabelecimento.MapaEstabelecimentoActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EstabelecimentoActivity"
            android:label="@string/title_activity_estabelecimento"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".estabelecimento.EstabelecimentoActivity"
            android:label="@string/title_activity_estabelecimento"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".estabelecimento.AlterarEstabelecimentoActivity"
            android:label="@string/title_activity_alterar_estabelecimento"
            android:theme="@style/AppTheme" />
        <activity android:name=".estabelecimento.MapaAlterarEstabelecimentoActivity"></activity>
    </application>

</manifest>